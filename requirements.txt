fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
celery==5.3.4
redis==5.0.1
psycopg2-binary==2.9.9
requests==2.31.0
python-dotenv==1.0.0
flower==2.0.1
sqlalchemy==2.0.23
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2
biopython==1.83
propka==3.5.0
anthropic>=0.40.0
modal>=0.73.0
# Local ESMFold inference (Celery worker only — ~2 GB download on first run)
transformers>=4.36.0
torch>=2.1.0
pyyaml>=6.0

meeko

# Boltz-2 (GPU required — install from source, not PyPI):
#   pip install git+https://github.com/jwohlwend/boltz
# Then set BOLTZ_ENABLED=True in .env

# Conda-managed (no ARM64 PyPI wheel) — installed in Dockerfiles via micromamba:
#   openmm, pdbfixer, openmmforcefields, rdkit
#   openff-toolkit, openff-forcefields, openff-interchange
#   vina
#
# Local install (outside Docker):
#   PyRosetta:  conda install -c rosettacommons pyrosetta
#   GROMACS:    brew install gromacs  (M3 Mac) / apt-get in Dockerfile.celery
#   MDAnalysis: conda install -c conda-forge mdanalysis