fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
celery==5.3.4
redis==5.0.1
psycopg2-binary==2.9.9
requests==2.31.0
python-dotenv==1.0.0
flower==2.0.1
sqlalchemy==2.0.23
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2
biopython==1.83
propka==3.5.0
anthropic>=0.40.0

openmm
pdbfixer
openmmforcefields
rdkit
openff-toolkit
openff-forcefields
vina
meeko
openff-interchange

# Tools not available on PyPI — install separately:
# PyRosetta:  conda install -c rosettacommons pyrosetta   (free academic license at rosettacommons.org)
# GROMACS:    brew install gromacs  (M3 Mac host)  or  see Dockerfile.celery for Docker
# OpenMM:     conda install -c conda-forge openmm          (Stage C — set OPENMM_ENABLED=True)
# MDAnalysis: conda install -c conda-forge mdanalysis      (optional trajectory analysis)