[pytest]
# A single unimportable module (e.g. a missing optional dep) must not abort the
# entire suite during collection — report the error and keep running the rest.
addopts = --continue-on-collection-errors
